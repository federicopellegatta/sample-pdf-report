spring:
  application:
    name: sample-pdf-report
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update

server:
  port: 8081

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
